# 固定 HTML 报告模板

最终交付为单文件 HTML。结构和样式由 `scripts/render_report.py` 唯一控制。

## 强制生成方式

1. 运行 `python scripts/render_report.py init <Skill名称> --output-dir <交付目录>`，生成完整 JSON 骨架；
2. 只填写生成的过程 JSON，不另写字段和页面结构；
3. 运行 `python scripts/render_report.py build <过程JSON路径>`；
4. `build` 一次列出全部校验问题；按清单修改 JSON 后重新运行；
5. 校验通过后，脚本自动把 HTML 生成到 `过程文件_CO/` 的上一级目录，并完成输出检查；
6. 禁止直接手写 HTML、CSS，禁止渲染后人工改报告正文。

只检查 JSON、不生成 HTML 时运行：

```bash
python scripts/render_report.py check <过程JSON路径>
```

确需指定 HTML 路径时运行：

```bash
python scripts/render_report.py build <过程JSON路径> --output <最终HTML路径>
```

只有用户明确要求改版式时，才修改渲染脚本。普通评估任务不得重新设计页面。

## 固定页面结构

### 页首

- 报告标题；
- 评估日期与范围；
- Skill 定位：3—4 个短句，分别说明真实身份和主要用途、适用需求、工作方式、交付结果。要从用户的角度出发阐述。参考以下示例：
示例：
啥好用是一个对 Skill 进行安装前评估的工作流。
啥好用帮助用户判断目标 Skill 是否适合自己的需求。
啥好用通过只读检查候选材料，评估用途、安装要求、费用、Token 消耗和风险。
最终用户可以得到一份 HTML 格式的 Skill 评估说明书。



### 1. 结论概述

顺序固定：

1. 推荐程度、判断口径与 10 格色条；
2. **单框三段**：最终结论 / 量化分析 / 适用场景，依次排在同一个框里，每段一个小标题加一段话，不再是并列卡片，也不再用 bullet list。

三段各自只做一件事：

- **最终结论**：一段话，第一句直接写匹配、部分匹配或不匹配；再写一个关键优势/最适合的点、一个最需要注意的点；
- **量化分析**：一段话，只挑最决定推荐分的 1—2 个数字，不把七项指标都摆一遍；
- **适用场景**：一句话，固定句式“（Skill名）适合……的用户，用来……”，禁止用“一句话：”这种开头词。

原来单独一张的“使用建议”卡片不再出现——是否建议安装、什么时候考虑、如何轻量试用，统一放到第 5 节“使用节奏建议”里展开，这里不重复。

### 2. 这个 Skill 是啥

固定一行结论：用“技术原理 + 展开的工作 + 实现的效果”三段式概括 Skill，例如“（Skill名）用……机制，展开……的工作，实现……的效果。”

固定四格，2×2 排列，模拟用户使用流程：

| ① 用户需求 | ② 任务步骤 | ③ 额外需要 | ④ 最后交付 |
|---|---|---|---|
| 什么时候调用 + 你要提供什么 | 3—5 个实际步骤 | 除 AI agent 本身外还要装/注册/申请/订阅什么 | 交付结果及其可用方式、主要作用 |

- **用户需求**：合并原来的“使用时机”和“用户提供”，用 2—6 个短句写用户遇到什么情况该调用、要提供什么链接/文件/材料；
- **任务步骤**：写 Skill 完成任务的 3—5 个核心动作，每条只写一个步骤；
- **额外需要**：只写**除 AI agent 本身外**还需要的东西——额外安装、注册、申请甚至订阅的软件或服务；不写“能执行命令行的 AI agent”这类前提条件，也不在这里展开完整安装清单；
- **最后交付**：用 2—4 个短句写 Skill 最终交付的结果、形式、可用方式和主要作用。

四张卡全部使用列表，每条不超过 35 个汉字。文件输入要写“文件”“本地文件夹”或具体链接，不写含义不明的“路径”。不用箭头连接四张卡，2×2 的阅读顺序（先看两行的左右，再看下一行）本身就足够表达先后关系。

### 3. 这个 Skill 好用吗

固定一行结论：按**完整度 → 好用度 → 优势和不足哪个权重更大**这个固定顺序写三件事；不判断是否匹配具体用户。行文里必须依次出现“完整度”和“好用度”这两个词（先完整度、后好用度），第三句点出优势和不足谁的分量更重，不能只是“优势+门槛”平铺两件事。

1. 定性评估
“好用之处 / 可能卡住”固定为左右两个卡片。

- **好用之处**：2—4 个平行短句，说明目标 Skill 的优势、亮点和好用之处；
- **可能卡住**：2—4 个平行短句，说明目标 Skill 可能产生的卡点、不足和风险。

2. 指标分析
固定为：
| 主要指标 | 评估结果 | 用户影响 |
|---|---|---|
| 公开关注度 | | |
| 下载量 | | |
| 安装复杂度 | | |
| 任务复杂度 | | |
| Skill 调用准确性 | | |
| 额外涉及费用 | | |
| Token 消耗 | | |
| 风险值 | | |

- **主要指标**：1）公开关注度写明可核验的 GitHub Stars、Forks、榜单或其他公开数据；2）下载量仅有公开数值时显示。
- **评估结果**：量化数据结果，如得分，简单/中等/复杂，低/中/高；评估方式等。指标分析表的“评估结果”列里，得分/评级本身（如“1.5/10｜很简单”“高关注”）会被渲染成强调红；如果后面跟着“｜按……估算”“｜根据……估算”这类估算方式说明，写的时候把它放在同一个字符串的最后一段，用“｜”分隔，渲染时会自动识别并改回黑色常规字重，不用手动处理。
- **用户影响**：包括对评估结果的解读，以对于用户来说意味着什么的说明。

3. 安装准备
固定为：
| 前期准备 | 是否必须 | 用户影响 |
|---|---|---|
| 额外软件安装 | | |
| 额外注册/登录的账号 | | |
| API KEY/联网服务 | | |
| 涉及的数据与隐私 | | |


### 4. 这个 Skill 适合你吗

固定一行结论：由“最终结论”第一句与“使用建议”第一句自动组成，使用与第 2、3 部分章节摘要相同的左侧朱砂线摘要条；
示例：`示例 Skill 不匹配你的需求。不建议安装示例 Skill。`

“适合场景 / 不适合场景”固定为左右两个卡片。
- **适合场景**：2—4 个平行短语，直接写任务、偏好或使用条件；
- **不适合场景**：2—4 个平行短语，直接写不匹配的任务、偏好或限制。
两组都不写 Skill 名称、“它”“用户”，也不重复“适合”“不适合”。
如果用户提出了自己的需求，要围绕用户需求进行适合与不适合场景的分析。

### 5. 使用节奏建议

固定为 3—5 个阶段：第一次怎么试、什么时候扩展、何时复盘、什么情况停用。

### 页尾

顺序固定：

1. 折叠显示来源与尚不能确认的事项；
2. 浅色“说明”，使用同层级列表：第一条解释本报告中的 AI agent，后续各条说明采用边界和安全提醒；不单设“名词说明”。证据边界必须兼容只读检查与正文明确记录的已授权实测，不得否定正文中的实测事实。

版式固定使用“白纸墨字”：页面底色 `#FFFFFF`（纯白），正文 `#17181A`（近黑），次要信息 `#676C74`（灰），细线 `#E3E5E8`，浅底 `#F5F6F7`；四个点缀色固定为砖红 `#B73F42`、深蓝 `#244C66`、暖金 `#D6A553`、灰绿 `#82B29B`。禁止由过程 JSON 自定义配色。

配色只是点缀，不是大面积铺色——页面上 80% 以上的字仍是黑/灰。四色只用在这几处：

- Skill 名称、推荐分数数字、推荐条填充色：固定用砖红；
- 五个章节标题前的数字徽章：按顺序循环“深蓝→灰绿→暖金→砖红→深蓝”，深蓝/砖红配白字，暖金/灰绿配黑字（因为底色浅，白字会糊）；
- 每章摘要行、结论概述框、这个 Skill 适合你吗的匹配结论框：左侧 3px 竖线用对应章节色，正文本身仍是黑色；
- 卡片、表格一律纯白底 + 细灰边框（`#E3E5E8`）+ 3px 装饰色左边条，不用半透明色块叠加，不用阴影；
- 指标分析表“评估结果”列，得分/评级本身标红加粗，跟在后面的“｜按……估算”说明文字改回黑色常规字重；
- 推荐条（10 格）用砖红单色由浅到深，不用多色渐变；
- 页首用一条 2px 黑色细线，不设装饰圆斑。

正文、列表、表格内容使用 `15px`；页首定位摘要与“这个 Skill 适合你吗”摘要使用 `16px`；页尾“说明”文字单独调小到 `12px`，明显小于正文一档。报告标题、章节标题、卡片小标题、表头和推荐分数保持各自字号，不随正文缩小。

## JSON Schema

```json
{
  "schema_version": 7,
  "output_name": "Skill名称-啥好用评估_CO.html",
  "title": "Skill名称｜啥好用评估",
  "date": "YYYY-MM-DD",
  "scope": "只读检查哪些文件与公开资料",
  "positioning": [
    "Skill名称是……。",
    "它通过……完成……。",
    "最终用户可以得到……。",
    "经评估，Skill名称更适合……。"
  ],
  "recommendation": {
    "score": 7.8,
    "label": "推荐",
    "basis_type": "target",
    "basis": "判断对象：准备安装 Skill 的普通用户",
    "final_conclusion": [
      "Skill名称适合这类用户。",
      "Skill名称的核心优势是……。",
      "用户需要注意……。"
    ],
    "quantitative_analysis": [
      "这次得分主要由安装复杂度、风险值这 1—2 个数字决定：……。"
    ],
    "use_cases": "Skill名称适合……的用户，用来……。",
    "advice": "建议安装它。"
  },
  "what": {
    "summary": "Skill名称用……机制，展开……的工作，实现……的效果。",
    "user_needs": ["用户遇到……", "用户提供……"],
    "steps": ["步骤1", "步骤2", "步骤3"],
    "extra_needs": ["除 AI agent 本身外，用户还需要……", "Skill名称还会调用……"],
    "output": ["用户可以得到……", "用户可以继续……"]
  },
  "quality_summary": "完整度……。好用度……。优势和不足相比，……的权重更大。",
  "metrics": [
    {"name": "公开关注度", "result": "高关注", "meaning": "GitHub Stars、Forks、榜单或其他公开数据及其含义"},
    {"name": "安装复杂度", "result": "4.3/10｜中等", "meaning": "人话解释"},
    {"name": "任务复杂度", "result": "5.2/10｜复杂｜根据公开文件估算", "meaning": "基准任务、主要操作与人工环节"},
    {"name": "Skill 调用准确性", "result": "偏宽", "meaning": "根据名称、描述和触发规则判断，说明最可能出现的误调用或漏调用"},
    {"name": "额外涉及费用", "result": "无新增费用", "meaning": "人话解释"},
    {"name": "Token 消耗", "result": "6.5/10｜高｜根据公开文件估算", "meaning": "核心任务与最高消耗环节"},
    {"name": "风险值", "result": "3.5/10｜较低", "meaning": "主要风险来源"}
  ],
  "strengths": ["好用点1", "好用点2"],
  "frictions": ["卡点1", "卡点2"],
  "preparation": [
    {"name": "额外软件安装", "required": "……", "impact": "……"},
    {"name": "额外注册/登录的账号", "required": "……", "impact": "……"},
    {"name": "API KEY/联网服务", "required": "……", "impact": "……"},
    {"name": "涉及的数据与隐私", "required": "……", "impact": "……"}
  ],
  "fit": {
    "good_for": ["希望完成的任务", "愿意接受的使用方式"],
    "not_for": ["无法完成的任务", "不能接受的使用条件"]
  },
  "usage_rhythm": ["第一次怎么试", "什么时候扩展", "什么时候停用"],
  "sources": [{"name": "项目 README", "url": "https://..."}],
  "unverified": ["尚不能确认的事项"]
}
```

## 字段口径

- 所有定量评分共用规则：见 [scoring-rubric.md](scoring-rubric.md)；
- 安装复杂度：见 [installation-score.md](installation-score.md)；
- 任务复杂度：见 [task-complexity.md](task-complexity.md)，必须按典型核心任务评分；
- Skill 调用准确性：见 [invocation-accuracy.md](invocation-accuracy.md)，根据名称、描述和触发规则作定性判断；
- Token 消耗：见 [token-score.md](token-score.md)，必须按典型核心任务评分；
- 风险值：见 [risk-score.md](risk-score.md)；
- 推荐程度：见 [fit-assessment.md](fit-assessment.md)；
- 所有用户可见文字：见 [writing-style.md](writing-style.md)。

许可证未验证时，额外费用不得写“核心免费”或“无新增费用”；应分别说明直接费用和使用许可。

推荐分数、最终结论第一句和使用建议必须一致（`advice` 现在是一句话字符串，不是列表）：

| 推荐分数 | 个性化判断第一句 | 目标人群判断第一句 | 使用建议（`advice`） |
|---|---|---|---|
| 0—3.9 | Skill名称不匹配你的需求。 | Skill名称不适合这类用户。 | 不建议安装它。 |
| 4.0—5.9 | Skill名称部分匹配你的需求。 | Skill名称部分适合这类用户。 | 建议先试用它，再决定是否安装。 |
| 6.0—7.4 | Skill名称部分匹配你的需求。 | Skill名称部分适合这类用户。 | 如果满足具体条件，可以安装它。 |
| 7.5—10 | Skill名称匹配你的需求。 | Skill名称适合这类用户。 | 建议安装它。 |

`basis_type` 只在过程 JSON 中使用：`personalized` 表示结合具体用户判断，`target` 表示按写明的人群判断。用户可见的 `basis` 必须直接说明依据：

- 个性化示例：`根据你的行研需求、Windows 和 Codex 环境判断`；
- 目标人群示例：`判断对象：准备安装 Skill 的普通用户`。

没有证据时写 `未验证`。下载量没有公开数值时，不创建该指标。

## 五个区域的唯一职责

| 区域 | 只负责 | 不负责 |
|---|---|---|
| 结论 | 是否匹配、是否安装、决定性理由 | 完整功能介绍、详细步骤、全部限制 |
| 这个 Skill 是啥 | 适用用户、调用场景、输入、工作过程、依赖与交付 | 推荐、评分和使用评价 |
| 这个 Skill 好用吗 | 整体完成度、质量表现、量化指标、卡点和安装准备 | 用户匹配与推荐结论 |
| 这个 Skill 适合你吗 | 当前需求与能力、交付、环境和代价的差异 | 重复指标、安装清单和完整功能 |
| 使用节奏建议 | 第一次试用、扩展、复盘和停用动作 | 再次解释为什么推荐 |

## 重复规则

### 允许重复

只允许四类摘要与详情重复：

1. 总结论与“这个 Skill 适合你吗”顶部结论必须完全一致；
2. 量化分析只报关键结果，指标表解释分数和原因；
3. 页首定位与“这个 Skill 是啥”一行结论可以概括同一身份，但四格只在第 2 部分展开；
4. “这个 Skill 好用吗”一行结论可以概括优势和门槛，完整依据只在双栏与指标表展开。

### 禁止重复

- 身份、工作方式和交付只在“这个 Skill 是啥”完整说明；
- 软件、账号、Key 和联网要求只在安装准备列清单；
- 分数原因只在指标表展开；
- 质量优势和真实卡点只放好用与卡点双栏；
- 许可证、测试、维护和兼容性等证据缺口只在“尚不能确认”列明；
- 第一次试用、扩展和停用动作只在使用节奏建议出现；
- 结论可以概括一个决定性限制，但不得复制其完整证据。
